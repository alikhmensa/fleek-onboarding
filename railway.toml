[build]
builder = "nixpacks"
buildCommand = "cd backend && pip install -r requirements.txt && python -m scripts.seed_inventory"

[build.env]
PYTHON_VERSION = "3.13"

[deploy]
startCommand = "cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "on-failure"
